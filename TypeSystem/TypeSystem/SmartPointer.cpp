#include "SmartPointer.h"
/*
template<typename T>
SmartPointer<T>::SmartPointer(T* pointer) : pointer_(move(pointer))
{
	
}


template<typename T>
SmartPointer<T>::~SmartPointer()
{
	
}

template<typename T>
SmartPointer::SmartPointer(SmartPointer <T> const& other) : mPointer(move(other.mPointer))
{

}

template<typename T>
SmartPointer::SmartPointer(SmartPointer <T> && otherSmartPointer)
{
	swap(mPointer, otherSmartPointer.mPointer);
}



template<typename T>
T* SmartPointer<T>::getObject()
{
	return pointer_;
}

template<typename T>
SmartPointer<T>::~SmartPointer()
{

}
*/