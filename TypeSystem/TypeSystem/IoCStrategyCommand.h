#ifndef _IOC_STRATEGY_COMMAND_H_
#define _IOC_STRATEGY_COMMAND_H_
/*
#include "IoCStrategy.h"
#include "Resolver.h"

class IoCStrategyCommand : public IoCStrategy
{
	Resolver resolver;
public:
	IoCStrategyCommand();
	virtual ~IoCStrategyCommand() noexcept;
	virtual void* Create(IObject const& args);

};
*/
#endif
