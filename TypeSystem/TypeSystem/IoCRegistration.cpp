#include "IoCRegistration.h"
/*
void RegistrateIoC(IoC &i)
{
	//i.Register(Pointer<IoCStrategyCommand()>, "Command");
	//i.Register(Pointer<IoCStrategyError>, "Nonexisting key");
	//...
	i.Register("Objects", Pointer<IoCStrategyObjects>(IoCStrategyObjects()) );
}
*/
